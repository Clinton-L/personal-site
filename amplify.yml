version: 1
frontend:
  phases:
    pre_build:
      commands:
        - npm ci --cache .npm --prefer-offline
        - npx ng version
    build:
      commands:
        - echo Build started on `date`
        - npx ng build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - .npm/**/*
