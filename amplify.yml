version: 1
frontend:
  phases:
    pre_build:
      commands:
        - npm install
        - npm install -g @angular/cli
    build:
      commands:
        - echo Build started on `date`
        - npm run build
  artifacts:
    baseDirectory: dist/personal-site/browser
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
